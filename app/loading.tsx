import React from 'react'

type Props = {}

const Loading = (props: Props) => {
    return (
        <div className='text-3xl font-bold flex justify-center w-full dark:bg-black items-center tracking-wider'>Loading ...</div>
    )
}

export default Loading